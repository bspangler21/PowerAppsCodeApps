# Screenshots

Add screenshots of the app's three tabs here so they render in the sample's README:

- `contacts.png` — the Contacts (CRUD) tab
- `accounts.png` — the Accounts (File & Image Attachments) tab
- `functions-actions.png` — the Functions & Actions tab

Capture each from the running app (`npm run dev` → Local Play, or the deployed app) and save it with
the matching file name.
